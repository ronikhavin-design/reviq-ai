# Core data
pandas==2.3.3
numpy==2.4.4

# Machine learning
scikit-learn==1.8.0
xgboost==3.2.0

# Explainability
shap==0.51.0

# Experiment tracking
mlflow==3.12.0

# RAG: local retrieval (no API required)
# sentence-transformers upgrades embeddings from TF-IDF to semantic automatically.
# Install it for better retrieval quality; the system falls back to TF-IDF without it.
# sentence-transformers==3.x.x

# API
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4

# Dashboard
streamlit==1.57.0
plotly==6.7.0

# Utilities
joblib==1.5.3
loguru==0.7.3
pyyaml==6.0.3
python-dotenv==1.2.2

# Testing
pytest==9.0.3
pytest-cov==7.1.0

# Optional: LLM answer generation in the Revenue Copilot.
# Without this, the Copilot runs in fallback mode and shows retrieved text summaries.
# openai>=1.0.0
